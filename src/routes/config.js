export const PORT= 5000;

// src/config.js

export const dbConfig = {
    user: 'postgres',         // Nombre de usuario de PostgreSQL
    host: 'localhost',        // Dirección del servidor de base de datos (en tu caso, `localhost` si estás ejecutando PostgreSQL localmente)
    database: 'hotel_chain',  // Nombre de la base de datos a la que te conectarás
    password: '1234567890', // Contraseña del usuario
    port: 5432,               // Puerto por defecto de PostgreSQL
  };
  